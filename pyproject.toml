[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "ripe-asset-sync"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = ["requests", "pyyaml"]
